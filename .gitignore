build/
log/
.serve.pid
.ask/
build/.tools/
.serve.stamp

# The sites are data, not source: each is its own git repository with its own
# history, so tracking them here would add broken gitlinks and nothing else.
sites/
# Per-site build locks. mkdir(2) is the primitive; nothing here outlives a run.
state/
